# Future Ideas

Speculative, not committed to any phase. Add to this list rather than
scope-creeping an in-progress phase.

- Textual-based interactive TUI mode alongside the Typer CLI, for browsing
  search results / library visually — only if it genuinely improves UX
  over Rich prompts (per the project's stated tech-stack preference).
- Plugin marketplace / discovery command (`anime plugins search <name>` →
  PyPI lookup for `animax-plugin-*` packages).
- Watch-together / sync playback across devices.
- Subtitle plugin category (fetch/sync external subtitles).
- Cloud sync of library/history across machines (opt-in, self-hosted or
  user-provided backend — never a mandatory first-party service).
