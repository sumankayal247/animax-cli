"""Composite widgets built from tables/panels/status — bigger than a single
reusable primitive, smaller than a whole command's output. E.g. the tree
layout `ui.doctor` uses for grouped check results.
"""
